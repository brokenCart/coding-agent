[project]
name = "coding-agent"
version = "0.1.0"
description = "A toy coding agent for python code."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]

[dependency-groups]
dev = [
    "isort>=8.0.1",
    "ruff>=0.15.21",
]
